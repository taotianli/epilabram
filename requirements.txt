# ============================================================
# PyTorch: 不在此处安装，请根据硬件单独安装。
#
# 标准 GPU（x86_64）：
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# GH200 / aarch64（CUDA 12.8）：
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# CPU only：
#   pip install torch torchvision torchaudio
# ============================================================

# Scientific computing
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0

# EEG / data
mne>=1.4
h5py>=3.9
einops>=0.6

# Model / training utilities
timm>=0.9
pyyaml>=6.0
tqdm>=4.0

# Visualization & logging
matplotlib>=3.7
wandb>=0.15

# Jupyter (notebook support)
jupyter>=1.0
ipykernel>=6.25
ipywidgets>=8.1
